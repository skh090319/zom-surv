// 에코 이펙트 고급 렌더러. 오브젝트 수를 늘리지 않고 레이어만 보강한다.
function echoDrawShard(x,y,size,angle,alpha){ctx.save();ctx.translate(x,y);ctx.rotate(angle);ctx.fillStyle=`rgba(116,218,255,${alpha*.45})`;ctx.strokeStyle=`rgba(211,173,255,${alpha})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(size,0);ctx.lineTo(-size*.55,size*.28);ctx.lineTo(-size*.25,-size*.42);ctx.closePath();ctx.fill();ctx.stroke();ctx.restore()}
function drawEchoEffectsV2(){
  if(selectedCharacter!=="echo")return;worldStart();ctx.save();const now=performance.now();ctx.globalCompositeOperation="lighter";
  for(const r of echoRifts){const alpha=Math.min(.78,r.life/38),dx=r.x2-r.x1,dy=r.y2-r.y1,len=Math.hypot(dx,dy)||1,nx=-dy/len,ny=dx/len;ctx.shadowColor="#7258ff";ctx.shadowBlur=10;ctx.strokeStyle=`rgba(64,113,255,${alpha*.48})`;ctx.lineWidth=9;ctx.beginPath();ctx.moveTo(r.x1,r.y1);ctx.lineTo(r.x2,r.y2);ctx.stroke();ctx.shadowBlur=0;ctx.strokeStyle=`rgba(224,252,255,${alpha})`;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(r.x1+nx*2,r.y1+ny*2);ctx.lineTo(r.x2+nx*2,r.y2+ny*2);ctx.stroke();ctx.strokeStyle=`rgba(184,104,255,${alpha*.75})`;ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(r.x1-nx*4,r.y1-ny*4);ctx.lineTo(r.x2-nx*4,r.y2-ny*4);ctx.stroke()}
  for(const k of echoKnots){const pulse=1+Math.sin(k.pulse)*.1,g=ctx.createRadialGradient(k.x,k.y,1,k.x,k.y,k.r);g.addColorStop(0,"rgba(240,255,255,.72)");g.addColorStop(.13,"rgba(110,95,255,.35)");g.addColorStop(.55,"rgba(80,35,180,.11)");g.addColorStop(1,"rgba(30,10,80,0)");ctx.fillStyle=g;ctx.beginPath();ctx.arc(k.x,k.y,k.r,0,Math.PI*2);ctx.fill();for(let n=0;n<3;n++){ctx.strokeStyle=`rgba(${n?180:80},${n?115:235},255,.82)`;ctx.lineWidth=2.5;ctx.beginPath();ctx.ellipse(k.x,k.y,(20+n*10)*pulse,(8+n*5)*pulse,now*.001*(n%2?1:-1)+n,0,Math.PI*2);ctx.stroke()}}
  for(const e of echoEffects){const p=1-e.life/e.maxLife,a=Math.max(0,1-p);if(e.type==="cut"){const dx=e.x2-e.x1,dy=e.y2-e.y1,len=Math.hypot(dx,dy)||1,nx=-dy/len,ny=dx/len;ctx.shadowColor="#c8faff";ctx.shadowBlur=15;ctx.strokeStyle=`rgba(235,253,255,${a})`;ctx.lineWidth=7*(1-p)+2;ctx.beginPath();ctx.moveTo(e.x1,e.y1);ctx.quadraticCurveTo((e.x1+e.x2)/2+nx*18*a,(e.y1+e.y2)/2+ny*18*a,e.x2,e.y2);ctx.stroke();ctx.shadowBlur=0;for(let n=0;n<5;n++){const t=(n+1)/6;echoDrawShard(e.x1+dx*t+nx*(n%2?10:-8)*p,e.y1+dy*t+ny*(n%2?10:-8)*p,5+n%3,Math.atan2(dy,dx)+p*2,a)}}else if(e.type==="phase"){const dx=e.x2-e.x1,dy=e.y2-e.y1;for(let n=0;n<4;n++){ctx.strokeStyle=`rgba(${110+n*25},${100+n*25},255,${a*(.55-n*.08)})`;ctx.lineWidth=13-n*3;ctx.setLineDash([18+n*4,12+n*3]);ctx.lineDashOffset=-p*90-n*12;ctx.beginPath();ctx.moveTo(e.x1,e.y1);ctx.lineTo(e.x2,e.y2);ctx.stroke()}ctx.setLineDash([]);for(let n=0;n<7;n++){const t=(n+p*.5)%1;echoDrawShard(e.x1+dx*t,e.y1+dy*t,7+n%3,Math.atan2(dy,dx)+n,a)}}else if(e.type==="snap"||e.type==="knotBorn"){const rr=(e.r||95)*Math.min(1,p*3);for(let n=0;n<3;n++){ctx.strokeStyle=`rgba(${n?190:80},${n?110:240},255,${a*(1-n*.18)})`;ctx.lineWidth=8-n*2;ctx.beginPath();ctx.arc(e.x,e.y,rr*(1-n*.12),p*4+n,p*4+n+Math.PI*1.5);ctx.stroke()}}else if(e.type==="close"){for(const r of e.rifts){ctx.shadowColor="#e8fbff";ctx.shadowBlur=18;ctx.strokeStyle=`rgba(235,250,255,${a})`;ctx.lineWidth=2+12*Math.sin(p*Math.PI);ctx.beginPath();ctx.moveTo(r.x1,r.y1);ctx.lineTo(r.x2,r.y2);ctx.stroke();ctx.shadowBlur=0}}else if(e.type==="collapse"){ctx.globalCompositeOperation="source-over";ctx.fillStyle=`rgba(28,8,58,${Math.sin(p*Math.PI)*.3})`;ctx.beginPath();e.knots.forEach((q,n)=>n?ctx.lineTo(q.x,q.y):ctx.moveTo(q.x,q.y));ctx.closePath();ctx.fill();ctx.globalCompositeOperation="lighter";for(let n=0;n<e.knots.length;n++){const q=e.knots[n],b=e.knots[(n+1)%e.knots.length];ctx.strokeStyle=`rgba(${n%2?190:75},${n%2?115:235},255,${a*.92})`;ctx.lineWidth=4+Math.sin(p*Math.PI)*12;ctx.beginPath();ctx.moveTo(q.x,q.y);ctx.quadraticCurveTo(e.cx,e.cy,b.x,b.y);ctx.stroke()}const rr=180*(1-p)+18,g=ctx.createRadialGradient(e.cx,e.cy,2,e.cx,e.cy,rr);g.addColorStop(0,`rgba(245,255,255,${a})`);g.addColorStop(.22,`rgba(155,100,255,${a*.65})`);g.addColorStop(1,"rgba(40,0,90,0)");ctx.fillStyle=g;ctx.beginPath();ctx.arc(e.cx,e.cy,rr,0,Math.PI*2);ctx.fill();for(let n=0;n<12;n++){const ang=n*Math.PI/6+p*3;echoDrawShard(e.cx+Math.cos(ang)*rr*.7,e.cy+Math.sin(ang)*rr*.7,10+n%4,ang+Math.PI/2,a)}}}
  ctx.restore();worldEnd();
}
